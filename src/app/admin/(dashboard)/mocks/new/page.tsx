import Link from "next/link";
import { createMockExamAction } from "@/app/admin/author-actions";
import { AdminPageHeader } from "@/components/admin-ui";
import { getSubjects, listMockTemplates } from "@/lib/data/authoring";

export const dynamic = "force-dynamic";

export default async function NewMockExamPage() {
  const [subjects, templates] = await Promise.all([getSubjects(), listMockTemplates()]);
  return <><AdminPageHeader eyebrow="模試制作" title="新しい模試を作成" description="テンプレート選択時は、問題数・試験時間・紙面設定を自動で読み込みます。" action={<Link className="btn-secondary" href="/admin/mocks">一覧へ戻る</Link>} /><div className="workbench max-w-4xl"><form action={createMockExamAction} className="card space-y-6 p-5 sm:p-7"><label><span className="label">模試名 *</span><input className="input" name="title" required placeholder="名古屋大学 数学予想模試 第1回" /></label><label><span className="label">テンプレート</span><select className="input" name="templateId" defaultValue=""><option value="">使わずに個別設定</option>{templates.map(({ template }) => <option key={template.id} value={template.id}>{template.name}（{template.questionCount}問 / {template.durationMinutes}分 / {template.paperSettings.paperSize}）</option>)}</select><span className="mt-1.5 block text-xs text-muted">選択した場合、下の基本設定よりテンプレートが優先されます。</span></label><div className="grid gap-5 sm:grid-cols-2"><label><span className="label">科目</span><select className="input" name="subjectId" defaultValue=""><option value="">未設定</option>{subjects.map((subject) => <option key={subject.id} value={subject.id}>{subject.name}</option>)}</select></label><label><span className="label">想定大学</span><input className="input" name="targetUniversity" placeholder="名古屋大学" /></label><label><span className="label">試験時間（分）</span><input className="input" name="durationMinutes" type="number" min="1" max="600" defaultValue="150" /></label><label><span className="label">大問数</span><input className="input" name="questionCount" type="number" min="1" max="20" defaultValue="4" /></label><label><span className="label">用紙</span><select className="input" name="paperSize" defaultValue="B5"><option value="B5">B5</option><option value="A4">A4</option></select></label><label><span className="label">文字サイズ</span><input className="input" name="fontSize" type="number" min="9" max="14" defaultValue="11" /></label><label><span className="label">余白（mm）</span><input className="input" name="marginMm" type="number" min="8" max="35" defaultValue="16" /></label><label><span className="label">段組</span><select className="input" name="columns" defaultValue="1"><option value="1">1段</option><option value="2">2段</option></select></label></div><div className="flex flex-wrap gap-5 text-sm"><label className="flex items-center gap-2"><input type="checkbox" name="showPageNumbers" defaultChecked />ページ番号</label><label className="flex items-center gap-2"><input type="checkbox" name="pageBreakPerProblem" />問題ごとに改ページ</label></div><button className="btn-primary w-full" type="submit">大問スロットを作成して編集へ</button></form></div></>;
}
